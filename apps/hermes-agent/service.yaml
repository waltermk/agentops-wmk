apiVersion: v1
kind: Service
metadata:
  name: hermes-agent-service
  namespace: ai-agents
spec:
  type: NodePort
  ports:
    - port: 9119
      targetPort: 9119
      nodePort: 30842
      name: gateway
  selector:
    app: hermes-agent