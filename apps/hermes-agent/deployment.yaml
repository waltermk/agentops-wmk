apiVersion: apps/v1
kind: Deployment
metadata:
  name: hermes-agent
  namespace: ai-agents
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: hermes-agent
  template:
    metadata:
      labels:
        app: hermes-agent
    spec:
      nodeSelector:
        kubernetes.io/hostname: k3s-dev
      containers:
        - name: agent
          image: nousresearch/hermes-agent:v2026.8.3
          args: ["serve", "--host", "0.0.0.0", "--port", "9119", "--skip-build"]
          ports:
            - containerPort: 9119
              name: serve
          env:
            - name: PORT
              value: "9119"
            - name: API_SERVER_ENABLED
              value: "true"
            - name: API_SERVER_HOST
              value: "0.0.0.0"
            - name: API_SERVER_PORT
              value: "9119"
            - name: API_SERVER_KEY
              valueFrom:
                secretKeyRef:
                  name: hermes-gateway-secret
                  key: token
            - name: HERMES_AUTH_TOKEN
              valueFrom:
                secretKeyRef:
                  name: hermes-gateway-secret
                  key: token
            - name: OLLAMA_BASE_URL
              value: "http://ollama-service.ai-agents.svc.cluster.local:11434"
            - name: MODEL_NAME
              value: "hermes3:8b"
            - name: GEMINI_API_KEY
              valueFrom:
                secretKeyRef:
                  name: external-ai-keys
                  key: gemini
                  optional: true
          volumeMounts:
            - name: agent-data
              mountPath: /opt/data
          resources:
            requests:
              cpu: "500m"
              memory: "512Mi"
      volumes:
        - name: agent-data
          persistentVolumeClaim:
            claimName: hermes-agent-pvc